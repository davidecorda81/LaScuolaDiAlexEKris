import { SyllableQuestion } from "./syllable-question.model";

describe('SyllableQuestion', () => {
  it('should create an instance', () => {
    expect(new SyllableQuestion()).toBeTruthy();
  });
});
